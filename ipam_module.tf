# ipam_module.tf (new file in root directory)
#module "ipam" {
 # source = "./modules/ipam"
  
  #delegated_account_id = var.delegated_account_id
  #aws_regions          = var.aws_regions
  #tfg_test_account1_id = aws_organizations_account.tfg-test-account1.id
  
  #providers = {
   # aws.delegated_account = aws.delegated_account
  #}
#}
